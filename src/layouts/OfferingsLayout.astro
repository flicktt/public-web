---
const { title } = Astro.props;
---

<div
  class="flex flex-col gap-8 lg:gap-12 max-w-section m-auto px-12 pt-6 pb-14"
>
  {title && <h2 class="font-bold text-center">{title}</h2>}
  <slot />
</div>
