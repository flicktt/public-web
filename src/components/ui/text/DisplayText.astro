---
import { cn } from "../../../utils";

interface Props extends astroHTML.JSX.HTMLAttributes {}

const props = Astro.props;
const classes = cn("font-light text-3xl sm:text-4xl", props?.class);
---

<p class={classes} {...props}>
  <slot />
</p>
